Keyword / filter chip. Selectable (filters) or removable (recipient lists, themes).

```jsx
<Tag selected onClick={()=>{}}>Communication</Tag>
<Tag onRemove={()=>remove(id)}>jordan@acme.com</Tag>
```

Props: `selected`, `onClick` (makes it clickable), `onRemove` (shows ×), `icon`.
