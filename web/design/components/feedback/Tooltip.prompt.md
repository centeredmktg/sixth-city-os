Lightweight tooltip for explaining icons, scores, and controls.

```jsx
<Tooltip content="Anonymous — names are never shown" side="top">
  <IconButton label="Info"><Info/></IconButton>
</Tooltip>
```

`side`: top / bottom / left / right. Wrap any focusable trigger.
