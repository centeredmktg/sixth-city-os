Checkbox for multi-select and consent. Supports a label + description.

```jsx
<Checkbox label="Keep responses anonymous" description="Recommended for candor" checked={a} onChange={e=>setA(e.target.checked)} />
```
