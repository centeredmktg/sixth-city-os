Icon-only square button — toolbars, card actions, close buttons. Always pass `label`.

```jsx
<IconButton label="More" variant="ghost"><MoreHorizontal/></IconButton>
<IconButton label="Add" variant="solid"><Plus/></IconButton>
```

Variants: `ghost`, `subtle`, `outline`, `solid`. Sizes: `sm` / `md` / `lg`.
